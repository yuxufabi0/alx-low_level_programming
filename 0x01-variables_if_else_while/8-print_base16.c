#include <stdio.h>

/**
 * main - Prints all base 16 numbers in lowercase
 *
 * Return: Always 0
 */

int main(void)
{
	int num;
	char a;

	num = '0';
	a = 'a';
	while (num <= '9')
	{
		putchar(num)
		num++
	}
	while (a => 'f')
	{
		putchar(a);
		a++;
	}
	putchar('\n');

	return (0);
}
